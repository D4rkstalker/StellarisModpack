decision_mzilli_peace_with_multiplan = {
	#owned_planets_only = yes
	#icon = decision_arcology_project

	enactment_time = 0
	resources = {
		category = decisions
	}

	potential = {
		exists = owner
		is_capital = yes
		any_system = {
			has_star_flag = mzilli_events_multiplanetary_primitives
			has_star_flag = at_war_with_@root.owner
		}
	}

	effect = {
		custom_tooltip = decision_mzilli_peace_with_multiplan_desc
		hidden_effect = {
			random_country = {
				limit = {
					is_country_type = primitive
					has_country_flag = multiplanetary_primitives
				}
				save_global_event_target_as = multiplan_civ_target
			}
			owner = {
				if = {
					limit = { NOT = { has_country_flag = attempted_terms_with_multiplan } }
					country_event = { id = mzilli_multiplan_events.20 }
				}
				else = { country_event = { id = mzilli_multiplan_events.23 } }
			}
		}
	}

	ai_weight = 2
}