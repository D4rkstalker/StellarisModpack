decision_security_drones = {
	owned_planets_only = yes
	sound = event_administrative_work
	
	resources = {
		category = decisions
		cost = {
			sr_wealth = 25
			alloys = 100
		}
	}
	
	potential = {
		not = { has_planet_flag = pf_filter_vanilla }
		not = { has_planet_flag = pf_security_drones }
		exists = owner
		owner = { 
			is_gestalt = no
			is_fallen_empire_spiritualist = no
			has_technology = tech_robotic_workers
		}
		num_buildings = { type = building_hall_judgment value > 0 }		
	}

	effect = {
		custom_tooltip = decision_security_drones_custom
		hidden_effect = {
			set_planet_flag = pf_security_drones
			check_planet_employment = yes
		}		
	}
	
	ai_weight = {
		weight = 0
	}
}

decision_no_security_drones = {
	owned_planets_only = yes
	sound = event_administrative_work
	
	resources = {
		category = decisions
		cost = {
		}
	}
	
	potential = {
		not = { has_planet_flag = pf_filter_vanilla }
		has_planet_flag = pf_security_drones	
	}

	effect = {
		custom_tooltip = decision_no_security_drones_custom
		hidden_effect = {
			remove_planet_flag = pf_security_drones
			check_planet_employment = yes
		}		
	}
	
	ai_weight = {
		weight = 100
	}
}

decision_credit_managers = {
	owned_planets_only = yes
	sound = event_administrative_work
	
	resources = {
		category = decisions
		cost = {
			sr_wealth = 25
		}
	}
	
	potential = {
		not = { has_planet_flag = pf_filter_vanilla }
		not = { has_planet_flag = pf_credit_managers }
		exists = owner
		owner = { 
			is_gestalt = no
			is_fallen_empire_spiritualist = no
		}
		num_districts = { type = district_generator value > 0 }		
	}

	effect = {
		custom_tooltip = decision_credit_managers_custom
		hidden_effect = {
			set_planet_flag = pf_credit_managers
			check_planet_employment = yes
		}		
	}
	
	ai_weight = {
		weight = 0
	}
}

decision_no_credit_managers = {
	owned_planets_only = yes
	sound = event_administrative_work
	
	resources = {
		category = decisions
		cost = {
			sr_wealth = 10
		}
	}
	
	potential = {
		not = { has_planet_flag = pf_filter_vanilla }
		has_planet_flag = pf_credit_managers	
	}

	effect = {
		custom_tooltip = decision_no_credit_managers_custom
		hidden_effect = {
			remove_planet_flag = pf_credit_managers
			check_planet_employment = yes
		}		
	}
	
	ai_weight = {
		weight = 100
	}
}