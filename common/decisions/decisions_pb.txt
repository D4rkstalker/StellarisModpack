#planetbuilders
decision_remove_built_planet = {
	icon = decision_exploding_planet
	potential = {
		exists = space_owner
		space_owner = { is_same_value = from }
		is_planet_class = pc_planet_blueprint
		num_pops = 0
	}
	
	effect = {
		hidden_effect = {
			FROM = {
				remove_country_flag = planet_placed
				save_event_target_as = planet_buyer
				capital_scope = {
					planet_event = { id = planet_builders.3 }
				}
			}
			remove_planet = yes
		}
	}
#AI always removes it, but doesn't get the ship back
	ai_weight = {
		weight = 10000
	}
}

decision_start_planet_building = {
	icon = decision_strategic_resources
	resources = {
		category = decisions
		cost = {
			energy = 4500
		}
	}
	
	potential = {
		AND = {
			exists = space_owner
			space_owner = { is_same_value = from }
			is_planet_class = pc_planet_blueprint
			num_pops = 0
		}
	}
	
	effect = {
		hidden_effect = {
			save_event_target_as = built_planet
			FROM = {
				save_event_target_as = planet_buyer
				remove_country_flag = not_started_building_yet
				country_event = { id = planet_builders.5 }
			}
			planet_event = { id = planet_builders.4 }
		}
	}
	
	ai_weight = {
		weight = 0
	}
}

decision_relocate_pb_base = {
	icon = decision_ship_in_orbit
	resources = {
		category = decisions
		cost = {
			influence = 20
		}
	}
	
	potential = {
		AND = {
			exists = space_owner
			space_owner = { is_same_value = from }
			space_owner = { has_country_flag = can_move_pb_station }
		}
	}
	
	effect = {
		hidden_effect = {
			space_owner = { remove_country_flag = can_move_pb_station }
			planet_event = { id = planet_builders.156 days = 1 }
		}
	}
	
	ai_weight = {
		weight = 0
	}
}

decision_edible_planet_change = {
	owned_planets_only = yes
	icon = decision_edible_planet_change

	potential = {
		owner = { is_regular_empire = yes }
		owner = { is_ai = no }
		is_planet_class = pc_pb_edible
	}

	effect = {
		change_pc = pc_pb_edible_cookies

	}
	
	ai_weight = {
		weight = 0
	}
}
decision_edible_planet_change_2 = {
	owned_planets_only = yes
	icon = decision_edible_planet_change

	potential = {
		owner = { is_regular_empire = yes }
		owner = { is_ai = no }
		is_planet_class = pc_pb_edible_cookies
	}

	effect = {
		change_pc = pc_pb_edible
	}
	
	ai_weight = {
		weight = 0
	}
}
