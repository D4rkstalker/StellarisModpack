decision_build_community_district = {
	resources = {
		category = decisions
		cost = {
			sr_wealth = 1
		}			
	}

	potential = {
		exists = owner
		not = { has_planet_flag = tpf_avoid_decision_spam }
		num_pops > 10
		free_jobs < 5
		has_swapped_communities = no
		num_districts = { type = district_community value < 1 }
		num_free_districts = { type = district_community value > 0 }
		owner = { 
			is_ai = yes
			is_regular_empire = yes
			is_gestalt = no
		}
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		add_district = district_community
	}
	
	ai_weight = {
		weight = 1000
	}
}

decision_create_local_industry = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = {
			sr_wealth = 1
		}			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		not = { has_planet_flag = tpf_create_local_industry }
		exists = owner
		free_building_slots > 1
		or = {
			num_districts = { type = district_industrialised value < 2 }
			num_buildings = { type = building_factory_1 value < 2 }
		}
		count_owned_pops = { limit = { is_unemployed = yes } count > 0 }
		owner = { 
			is_ai = yes
			is_regular_empire = yes
			has_monthly_income = {
				resource = consumer_goods
				value < 10
			}
			has_monthly_income = {
				resource = minerals
				value > 50
			}
		}
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_create_local_industry
			days = 360
		}
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		add_building = building_factory_1
	}
	
	ai_weight = {
		weight = 1000
	}
}

decision_create_crisis_police_force_from_artisans = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = {
			sr_wealth = 1
		}			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		not = { has_planet_flag = tpf_create_police_force }
		exists = owner
		owner = { 
			is_ai = yes
			is_regular_empire = yes
		}
		planet_crime > 15
		not = { has_building = building_precinct_house }
		not = { has_building = building_hall_judgment }
		has_building = building_factory_1
		owner = { 
			has_monthly_income = {
				resource = consumer_goods
				value > 15
			}
		}
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_create_police_force
			days = 360
		}
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		remove_building = building_factory_1
		add_building = building_precinct_house
	}
	
	ai_weight = {
		weight = 500
	}
}

decision_create_crisis_entertainment_from_artisans = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = {
			sr_wealth = 1
		}			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		exists = owner
		owner = { 
			is_ai = yes
			is_regular_empire = yes
		}
		free_amenities < 10
		not = { has_building = building_holo_theatres }
		not = { has_building = building_hyper_entertainment_forum }
		has_building = building_factory_1
		owner = { 
			has_monthly_income = {
				resource = consumer_goods
				value > 80
			}
		}
	}
	
	effect = {
		remove_building = building_factory_1
		add_building = building_holo_theatres
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
	}
	
	ai_weight = {
		weight = 250
	}
}

decision_create_police_force = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = {
			sr_wealth = 1
		}			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		not = { has_planet_flag = tpf_create_police_force }
		exists = owner
		owner = { 
			is_ai = yes
			is_regular_empire = yes
		}
		free_building_slots > 0
		planet_crime > 10
		count_owned_pops = { limit = { is_unemployed = yes } count > 0 }
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_create_police_force
			days = 360
		}
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		add_building = building_precinct_house
	}
	
	ai_weight = {
		weight = 500
	}
}

decision_clear_slots_for_better_stuff = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = {
			sr_wealth = 1
		}			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { is_ai = yes }
		free_building_slots < 1
		num_pops > 40
		or = {
			has_building = building_family_planning
			has_building = building_immigration_center
		}
	}
	
	effect = {
		remove_building = building_family_planning
		remove_building = building_immigration_center
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
	}
	
	ai_weight = {
		weight = 1000
	}
}

decision_emergency_food_bank = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = {
			sr_wealth = 1
		}			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		exists = owner
		owner = { 
			is_regular_empire = yes
			country_uses_food = yes
			is_ai = yes
			has_monthly_income = {
				resource = food
				value < 1
			}
		}
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		if = {
			limit = {
				num_free_districts = { type = district_farming value > 0 }
				count_owned_pops = { limit = { is_unemployed = yes } count > 0 }
			}
			add_district = district_farming
		}
		else = {
			if = {
				limit = {
					free_building_slots > 0
				}
				add_building = building_food_bank
			}
		}
	}
	
	ai_weight = {
		weight = 1000
	}
}

decision_emergency_goods_bank = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = {
			sr_wealth = 1
		}			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		exists = owner
		owner = { 
			is_ai = yes
			is_regular_empire = yes
			country_uses_consumer_goods = yes
			has_monthly_income = {
				resource = consumer_goods
				value < 1
			}
		}
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		if = {
			limit = {
				free_building_slots > 1
				count_owned_pops = { limit = { is_unemployed = yes } count > 2 }
			}
			add_building = building_factory_1
		}
		else = {
			if = {
				limit = {
					num_free_districts = { type = district_industrialised value > 0 }
					count_owned_pops = { limit = { is_unemployed = yes } count > 0 }
				}
				add_district = district_industrialised
			}
			else = {
				if = {
					limit = {
						free_building_slots > 0
					}
					add_building = building_goods_bank
				}
			}
		}
	}
	
	ai_weight = {
		weight = 500
	}
}

decision_counter_crime_reduce_growth = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = {
			sr_wealth = 1
		}			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { 
			is_ai = yes
			is_regular_empire = yes
			is_gestalt = no
		}
		planet_crime > 25
		or = {
			has_building = building_family_planning
			has_building = building_immigration_center
		}
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		remove_building = building_family_planning
		remove_building = building_immigration_center
		add_building = building_precinct_house
	}
	
	ai_weight = {
		weight = 1000
	}
}

decision_resolve_amenities_crisis = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		exists = owner
		owner = { 
			is_regular_empire = yes
			is_ai = yes
			is_gestalt = no
		}
		free_building_slots > 0
		free_amenities < -1
		not = { has_modifier = resolve_amenities_crisis }
		count_owned_pops = { limit = { is_unemployed = yes } count > 0 }
		num_buildings = { type = building_holo_theatres value < 1 }
		num_buildings = { type = building_hyper_entertainment_forum value < 1 }
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		add_modifier = {
			modifier = resolve_amenities_crisis
			days = 720
		}
		add_building = building_holo_theatres
	}
	
	ai_weight = {
		weight = 100
		modifier = {
			factor = 5
			free_amenities < -2
		}
		modifier = {
			factor = 0
			or = {
				num_buildings = { type = building_holo_theatres value > 0 }
				num_buildings = { type = building_hyper_entertainment_forum value > 0 }
			}
		}
	}
}

decision_fix_too_many_miners_need_food = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		exists = owner
		owner = { 
			is_regular_empire = yes
			country_uses_food = yes
			is_ai = yes
			has_monthly_income = {
				resource = minerals
				value > 75
			}
			has_monthly_income = {
				resource = food
				value < 25
			}
		}
		not = { has_planet_flag = pf_no_em_district_swapping }
		num_districts = { type = district_mining value > 1 }
		num_free_districts = { type = district_farming value > 0 }
	}
	
	effect = {
		remove_district = district_mining
		add_district = district_farming
		set_timed_planet_flag = {
			flag = pf_no_em_district_swapping
			days = 90
		}
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
	}
	
	ai_weight = {
		weight = 500
	}
}

decision_fix_too_many_miners_need_energy = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		exists = owner
		owner = { 
			is_regular_empire = yes
			is_ai = yes
			has_monthly_income = {
				resource = minerals
				value > 75
			}
			has_monthly_income = {
				resource = energy
				value < 25
			}
		}
		not = { has_planet_flag = pf_no_em_district_swapping }
		num_districts = { type = district_mining value > 1 }
		num_free_districts = { type = district_generator value > 0 }
	}
	
	effect = {
		remove_district = district_mining
		add_district = district_generator
		set_timed_planet_flag = {
			flag = pf_no_em_district_swapping
			days = 90
		}
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
	}
	
	ai_weight = {
		weight = 1000
	}
}

decision_fix_too_many_manufacturers_need_energy = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		exists = owner
		owner = { 
			is_regular_empire = yes
			is_ai = yes
			country_uses_consumer_goods = yes
			has_monthly_income = {
				resource = consumer_goods
				value > 30
			}
			has_monthly_income = {
				resource = energy
				value < 50
			}
		}
		free_jobs > 3
		num_districts = { type = district_industrialised value > 1 }
		num_free_districts = { type = district_generator value > 0 }
		num_districts = { type = district_generator value < 4 }
	}
	
	effect = {
		remove_district = district_industrialised
		add_district = district_generator
		set_timed_planet_flag = {
			flag = pf_no_industrial_districts
			days = 720
		}
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
	}
	
	ai_weight = {
		weight = 30
	}
}

decision_fix_too_many_manufacturers_need_food = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { 
			is_regular_empire = yes
			country_uses_consumer_goods = yes
			country_uses_food = yes
			is_ai = yes
			has_monthly_income = {
				resource = consumer_goods
				value > 30
			}
			has_monthly_income = {
				resource = food
				value < 50
			}
		}
		free_jobs > 3
		num_districts = { type = district_industrialised value > 0 }
		num_free_districts = { type = district_farming value > 0 }
		num_districts = { type = district_farming value < 4 }
	}
	
	effect = {
		remove_district = district_industrialised
		add_district = district_farming
		set_timed_planet_flag = {
			flag = pf_no_industrial_districts
			days = 720
		}
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
	}
	
	ai_weight = {
		weight = 20
	}
}

decision_fix_too_many_manufacturers_need_miners = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { 
			is_regular_empire = yes
			is_ai = yes
			country_uses_consumer_goods = yes
			has_monthly_income = {
				resource = consumer_goods
				value > 30
			}
			has_monthly_income = {
				resource = minerals
				value < 50
			}
		}
		free_jobs > 3
		num_districts = { type = district_industrialised value > 0 }
		num_free_districts = { type = district_mining value > 0 }
		num_districts = { type = district_mining value < 3 }
	}
	
	effect = {
		remove_district = district_industrialised
		add_district = district_mining
		set_timed_planet_flag = {
			flag = pf_no_industrial_districts
			days = 720
		}
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
	}
	
	ai_weight = {
		weight = 10
	}
}

decision_scrap_industrial_districts = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { 
			is_regular_empire = yes
			is_ai = yes
			country_uses_consumer_goods = yes
			country_uses_food = yes
			has_monthly_income = {
				resource = consumer_goods
				value > 50
			}
			has_monthly_income = {
				resource = alloys
				value > 25
			}
			or = {
				has_monthly_income = {
					resource = energy
					value < 100
				}	
				has_monthly_income = {
					resource = minerals
					value < 10
				}
				has_monthly_income = {
					resource = food
					value < 50
				}				
			}
		}
		free_jobs > 5
		num_districts = { type = district_industrialised value > 0 }
	}
	
	effect = {
		remove_district = district_industrialised
		set_timed_planet_flag = {
			flag = pf_no_industrial_districts
			days = 1800
		}
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
	}
	
	ai_weight = {
		weight = 10
	}
}

decision_resolve_stability_crisis = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { 
			is_regular_empire = yes
			is_ai = yes
		}
		planet_stability < 50
		not = { has_modifier = resolve_stability_crisis }
	}
	
	effect = {
		add_modifier = {
			modifier = resolve_stability_crisis
			days = 1800
		}
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
	}
	
	ai_weight = {
		weight = 25
		modifier = {
			factor = 2
			planet_stability < 10			
		}
		modifier = {
			factor = 2
			planet_stability < 20			
		}
		modifier = {
			factor = 0.5
			planet_stability > 30			
		}
		modifier = {
			factor = 0.1
			planet_stability > 45			
		}
		modifier = {
			factor = 0
			planet_stability > 50			
		}
	}
}

decision_resolve_employment_crisis = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { 
			is_regular_empire = yes
			is_ai = yes
		}
		num_pops > 25
		not = { has_modifier = resolve_employment_crisis }
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		add_modifier = {
			modifier = resolve_employment_crisis
			days = 1800
		}
		if = {
			limit = {
				free_building_slots > 1
				exists = owner
				owner = {
					country_uses_consumer_goods = yes
					has_monthly_income = {
						resource = consumer_goods
						value < 20
					}
				}
			}
			add_building = building_factory_1
		}
	}
	
	ai_weight = {
		weight = 0
		modifier = {
			weight = 1
			count_owned_pops = { 
				limit = { 
					is_unemployed = yes 
				}
				count > 2
			}
		}
		modifier = {
			weight = 3
			count_owned_pops = { 
				limit = { 
					is_unemployed = yes 
				}
				count > 3
			}
		}
		modifier = {
			weight = 5
			count_owned_pops = { 
				limit = { 
					is_unemployed = yes 
				}
				count > 4
			}
		}
		modifier = {
			weight = 10
			count_owned_pops = { 
				limit = { 
					is_unemployed = yes 
				}
				count > 5
			}
		}
	}
}

decision_upgrade_factories_1 = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { 
			is_regular_empire = yes
			is_ai = yes
			has_technology = tech_luxuries_1
			has_technology = tech_rare_crystals
			country_uses_consumer_goods = yes
		}
		num_buildings = { type = building_factory_1 value > 1 }
		num_buildings = { type = building_factory_2 value < 2 }
		num_buildings = { type = building_crystal_plant value < 2 }
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		remove_building = building_factory_1
		remove_building = building_factory_1
		add_building = building_factory_2
		add_building = building_crystal_plant
	}
	
	ai_weight = {
		weight = 500
	}
}

decision_upgrade_factories_2 = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { 
			is_regular_empire = yes
			is_ai = yes
			country_uses_consumer_goods = yes
			has_technology = tech_luxuries_2
			has_technology = tech_rare_crystals
		}
		num_buildings = { type = building_factory_2 value > 0 }
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		remove_building = building_factory_2
		add_building = building_factory_3
	}
	
	ai_weight = {
		weight = 500
	}
}

decision_upgrade_foundries_1 = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { 
			is_regular_empire = yes
			is_ai = yes
			has_technology = tech_alloys_1
			has_technology = tech_volatile_motes
		}
		num_buildings = { type = building_foundry_1 value > 1 }
		num_buildings = { type = building_foundry_2 value < 2 }
		num_buildings = { type = building_chemical_plant value < 2 }
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		remove_building = building_foundry_1
		remove_building = building_foundry_1
		add_building = building_foundry_2
		add_building = building_chemical_plant
	}
	
	ai_weight = {
		weight = 500
	}
}

decision_upgrade_foundries_2 = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { 
			is_regular_empire = yes
			is_ai = yes
			has_technology = tech_alloys_2
			has_technology = tech_volatile_motes
		}
		num_buildings = { type = building_foundry_2 value > 0 }
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		remove_building = building_foundry_2
		add_building = building_foundry_3
	}
	
	ai_weight = {
		weight = 500
	}
}

decision_resolve_housing_crisis = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { 
			is_regular_empire = yes
			is_gestalt = no
			is_ai = yes
		}
		num_free_districts = { type = district_city value > 1 }
		not = { has_planet_flag = mega_cities }
		free_housing < 0
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		add_district = district_city
		add_district = district_city
	}
	
	ai_weight = {
		weight = 500
	}
}

decision_resolve_housing_crisis_2 = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { 
			is_regular_empire = yes
			is_gestalt = no
			is_ai = yes
			has_monthly_income = {
				resource = energy
				value > 50
			}
		}
		free_district_slots < 1
		not = { has_planet_flag = mega_cities }
		free_housing < 0
		num_districts = { type = district_generator value > 4 }
		num_districts = { type = district_city value > 0 }
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		remove_district = district_generator
		add_district = district_city
	}
	
	ai_weight = {
		weight = 100
	}
}

decision_resolve_housing_crisis_3 = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { 
			is_regular_empire = yes
			is_ai = yes
			is_gestalt = no
		}
		free_district_slots < 1
		not = { has_planet_flag = mega_cities }
		free_housing < 0
		num_districts = { type = district_community value > 0 }
		num_districts = { type = district_city value > 0 }
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		remove_district = district_community
		add_district = district_city
	}
	
	ai_weight = {
		weight = 200
	}
}

decision_resolve_housing_crisis_4 = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { 
			is_regular_empire = yes
			is_gestalt = no
			is_ai = yes
			country_uses_food = yes
			has_monthly_income = {
				resource = food
				value > 50
			}
		}
		free_district_slots < 1
		not = { has_planet_flag = mega_cities }
		free_housing < 0
		num_districts = { type = district_farming value > 2 }
		num_districts = { type = district_city value > 0 }
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		remove_district = district_farming
		add_district = district_city
	}
	
	ai_weight = {
		weight = 300
	}
}

decision_resolve_housing_crisis_5 = {
	owned_planets_only = yes
	sound = event_administrative_work
	icon = decision_resources

	resources = {
		category = decisions
		cost = { sr_wealth = 1 }			
	}
	
	potential = {
		not = { has_planet_flag = tpf_avoid_decision_spam }
		owner = { 
			is_regular_empire = yes
			is_gestalt = no
			is_ai = yes
			has_monthly_income = {
				resource = minerals
				value > 50
			}
		}
		free_district_slots < 1
		not = { has_planet_flag = mega_cities }
		free_housing < 0
		num_districts = { type = district_mining value > 4 }
		num_districts = { type = district_city value > 0 }
	}
	
	effect = {
		set_timed_planet_flag = {
			flag = tpf_avoid_decision_spam
			days = 360
		}
		remove_district = district_mining
		add_district = district_city
	}
	
	ai_weight = {
		weight = 50
	}
}