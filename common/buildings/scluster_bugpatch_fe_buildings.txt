# Fallen empire capitals don't pass has_upgraded_capital.
# Choosing to create duplicate buildings for this very specific scenario rather than overwriting, though.

building_planetary_shield_generator_fe = {
	icon = building_planetary_shield_generator
	base_buildtime = @b3_time
	base_cap_amount = 1
	position_priority = 100

	category = army

	potential = {
		exists = owner
		owner = { is_fallen_empire = yes }
	}

	allow = {
		scluster_has_fe_capital_building = yes # has_upgraded_capital = yes
		# hidden_trigger = { owner = { is_ai = no } }
		if = {
			limit = {
				exists = orbital_defence
			}
			orbital_defence = {
				exists = starbase
				starbase = {
					NOR = {
						has_starbase_building = ring_orbital_shield_generator
						is_starbase_building_building = ring_orbital_shield_generator
					}
				}
			}
		}
	}

	# destroy_trigger = {
	# 	exists = owner
	# 	owner = { is_ai = yes }
	# }

	convert_to = { building_planetary_shield_generator }

	planet_modifier = {
		planet_orbital_bombardment_damage = -0.5
	}

	resources = {
		category = planet_buildings
		cost = {
			minerals = @b2_minerals
		}
		upkeep = {
			energy = @b2_upkeep
		}
	}

	prerequisites = {
		"tech_planetary_shield_generator"
	}
	show_tech_unlock_if = { is_fallen_empire = yes }
}