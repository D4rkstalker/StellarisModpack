# Filename:		scluster_game_rule_overwrites.txt
# Contributors:	SirBlackAxe

### Notes
# LIOS

# this = system
# root = country
# system_blocks_sensors = {
# 	OR = {
# 		is_inside_nebula = yes
# 		has_modifier = space_storm
#		has_modifier = zroni_storm
# 		has_star_flag = lcluster
# 		has_star_flag = sealed_system

# 		# count_starbase_buildings = { # Added
# 		# 	type = subspace_fluctuator
# 		# 	count > 0
# 		# }
# 	}
# }
# Root = country attempting to target system for movement
# This = system being targeted
can_enter_system_by_jump = {
	custom_tooltip = {
#		success_text = "debug: can jump"
		fail_text = cannot_jump_to_system

		# Sealed system
		OR = {
			NOT = { has_star_flag = sealed_system }
			AND = {
				root = { has_country_flag = entered_sealed_system }
				has_star_flag = sealed_system
			}
		}

		# L-Cluster
		NOT = { has_star_flag = lcluster }
	}
# 
	custom_tooltip = { # Added
		fail_text = cannot_jump_to_scluster_system_during_storm

		# Storm Cluster
		NAND = {
			has_global_flag = ongoing_space_storm
			# OR = {
				has_star_flag = scluster
			# 	has_star_flag = scluster_entrance
			# }
		}
	}
	# custom_tooltip = { # Added
	# 	fail_text = cannot_jump_to_subspace_fluctuator_system_during_storm

	# 	# Subspace Fluctuator
	# 	NAND = {
	# 		has_global_flag = ongoing_space_storm
	# 		count_starbase_buildings = {
	# 			type = subspace_fluctuator
	# 			count > 0
	# 		}
	# 	}
	# }
}
