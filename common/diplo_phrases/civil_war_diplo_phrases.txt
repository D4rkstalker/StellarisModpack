###
# FROM is the actor ie the initiator of the diplomatic action
# ROOT is the recipient of the diplomatic action

greetings = {

	### HOSTILE GREETINGS

	# Hostile Generic 1
	01_CIVWAR_GREETING = {
		trigger = {
			AND = {
				has_origin = origin_civwar
				is_at_war_with = FROM
				FROM = {
					has_country_flag = in_civil_war
				}
			}
		}
	}
}