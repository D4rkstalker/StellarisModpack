r_machinedlcmod_machine_core = {
	activation_duration = 1800
	portrait = "GFX_relic_machine_core"
	sound = "relic_activation_cybrex_war_forge"

	resources = {
		category = relics
		cost = { energy = 2000 }
	}
	
	ai_weight = {
		weight = 0
		modifier = {
			weight = 100
			owner = {
				has_monthly_income = {
					resource = energy
					value < 0
				}
			}
		}
		modifier = {
			weight = 0
			owner = {
				has_monthly_income = {
					resource = processing_power
					value < 3000
				}
			}
		}
	}
	
	triggered_country_modifier = {
		potential = { always = yes }
		country_base_processing_power_produces_add = 30
	}
	
	score = 2000

	active_effect = {
		add_modifier = { modifier = mod_machine_core_relic_research_bonus days = 1800 }
		custom_tooltip = relic_triumph_cooldown_short
		hidden_effect = {
			add_modifier = { modifier = "relic_activation_cooldown" days = 1800 }
			if = {
				limit = { NOT = { has_country_flag = relic_machine_core_activated } }
				set_timed_country_flag = { flag = relic_machine_core_activated days = 1800 }
			}
		}
	}

	# Possible check for activation
	possible = {
		custom_tooltip = {
			fail_text = "requires_relic_no_cooldown"
			NOT = { has_modifier = relic_activation_cooldown }
		}
	}
}