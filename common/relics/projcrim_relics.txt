@activation_cost = 3000
@triumph_duration = 3600
@triumph_duration_short = 1800
@triumph_duration_veryshort = 900

r_throng_queen_synapse = {
	activation_duration = @triumph_duration
	portrait = "GFX_throng_queen_synapse"
	sound = "relic_activation_prethoryn_blood_queen"

	resources = {
		category = relics
		cost = {
			unity = 2000
		}
	}
	
	ai_weight = {
		weight = 75
		modifier = {
			factor = 0.5
			is_at_war = no
		}
		modifier = {
			factor = 3
			is_authoritarian = yes
		}
	}
	
	triggered_country_modifier = {
		potential = {
			NOT = { has_authority = auth_machine_intelligence }
		}
		pop_growth_speed = 0.05
		country_unity_produces_mult = 0.10
	}

	triggered_country_modifier = {
		potential = {
			has_authority = auth_machine_intelligence
		}
		planet_pop_assembly_mult = 0.05
		country_unity_produces_mult = 0.10
	}

	score = 3000

	active_effect = {
		custom_tooltip = relic_throng_relic_effect
		custom_tooltip = relic_triumph_cooldown
		hidden_effect = {
			add_modifier = {
				modifier = "relic_activation_cooldown" 
				days = @triumph_duration
			}
			set_country_flag = not_used_throng_relic
			set_timed_country_flag = { flag = throng_relic_activated days = @triumph_duration }	
		}
	}

	# Possible check for activation
	possible = {
		custom_tooltip = {
			fail_text = "requires_relic_no_cooldown"
			NOT = { has_modifier = relic_activation_cooldown }
		}
	}
}