
#From = Planet scope
#This = Fleet scope
on_fleet_enter_orbit = {
	events = {
		pas.0 #the event will fire even if the bombardment stance is 'selective'
	}
}

# Planet has taken damage from orbital bombardment
# This = Planet
# From = Bombarder
on_planet_bombarded = {
	events = {
		pas.1 #the event won't fire if the bombardment stance is 'selective' #pdx why? 'selective' bombing isn't bombing?
	}
}

# A war has ended
# Root = Loser
# From = Main Winner
on_war_ended = {
	events = {
		pas.3
	}
}

# A planets controller becomes a country not the same as the owner.
# Root = Planet
# From = Planet Owner
# FromFrom = Planet Controller (the one occupying)
on_planet_occupied = {
	events = {
		pas.4
	}
}