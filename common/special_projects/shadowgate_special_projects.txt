special_project = {
	key = "SHADOW_REMNANT_PROJECT"
	cost = 0
	days_to_research = 1800
	picture = GFX_evt_shadow_remnant

	event_scope = planet_event

	abort_trigger = { exists = event_target:shadowgate_ap_country has_communications = event_target:shadowgate_ap_country }

	on_success = {
		owner = { country_event = { id = shadowgate_galactic.44 } }
	}
}


special_project = {
	key = "SHADOWGATE_FORGOTTEN_ANCHOR_PROJECT"
	cost = 0
	days_to_research = 240
	tech_department = engineering_technology
	picture = GFX_evt_glitchy_matrix

	event_scope = ship_event

	abort_trigger = { fromfrom = { NOR = { shadowgate_is_disabled = yes owner = { is_same_value = prevprev } } } }

	requirements = {
		shipclass_constructor = 1
	}

	on_success = {
		from = { planet_event = { id = shadowgate_galactic.510 days = 1 } }
	}

	on_fail = {
		remove_country_flag = shadowgate_random_event_shadow_anchor
	}

	on_cancel = {
		remove_country_flag = shadowgate_random_event_shadow_anchor
	}
}

special_project = {
	key = "SHADOWGATE_DARK_LODESTONE_PROJECT"
	cost = 0
	days_to_research = 120
	picture = GFX_evt_analyzing_artifacts

	event_scope = ship_event

	abort_trigger = {
		NOT = { exists = event_target:shadowgate_dl_planet }
	}

	requirements = {
		shipclass_science_ship = 1
	}

	on_success = {
		if = { limit = { owner = { NOT = { is_ai = yes } any_owned_planet = { is_colonized_shadowgate = yes } } }
			random_list = {
				100 = { ship_event = { id = shadowgate_galactic.405 } }
				5 = { modifier = { factor = 0 owner = { has_relic = r_shadowgate_es } } ship_event = { id = shadowgate_galactic.611 } }
				1 = { modifier = { factor = 0 OR = { has_global_flag = shadowgate_shadowforge_acquired owner = { has_relic = r_shadowgate_shadowforge } } } set_global_flag = shadowgate_shadowforge_acquired ship_event = { id = shadowgate_galactic.600 } }
			}
		}
		else = { ship_event = { id = shadowgate_galactic.405 } }
	}
}