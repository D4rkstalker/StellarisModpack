# auto_renew_decision = {
	# random_owned_planet = {
	#	limit = { has_planet_flag = pf_auto_renew }
	#	if = {
	#		limit = {
				# Check if an expiring decision was enacted
				# has_planet_flag = pf_had_decision_x
				# Check decision has expired
				# not = { has_modifier = modifier x }
				# owner = {
					# check if decision is affordable
	#				resource_stockpile_compare = {
	#					resource = sr_wealth
	#					value > 500
	#				}
	#			}
			#}
			# Renew the decision
			# add_modifier = {
			#	modifier = relevant_decision_modifier
			#	days = -1
			# }	
	#	}
	#	if = {
	#		limit = {
				# Check if an expiring decision was enacted
				# has_planet_flag = pf_had_decision_x
				# Check decision has expired
				# not = { has_modifier = modifier x }
	#			owner = {
					# check if decision is affordable
	#				resource_stockpile_compare = {
	#					resource = sr_wealth
	#					value > 500
	#				}
	#			}
	#		}
			# Renew the decision
			# add_modifier = {
			#	modifier = relevant_decision_modifier
			#	days = -1
			# }	
	#	}
	#}
#}