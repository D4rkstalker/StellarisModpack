# This: Country
# PREVPREV: Pop

eac_try_move_pop_planet_farming_hab_any = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            has_modifier = eac_apm_focus_food_modifier
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_farming_hab_60 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            has_modifier = eac_apm_focus_food_modifier
            habitability = { who = PREVPREV value >= 0.6 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_farming_hab_80 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            has_modifier = eac_apm_focus_food_modifier
            habitability = { who = PREVPREV value >= 0.8 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_mining_hab_any = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            has_modifier = eac_apm_focus_minerals_modifier
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_mining_hab_60 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            has_modifier = eac_apm_focus_minerals_modifier
            habitability = { who = PREVPREV value >= 0.6 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_mining_hab_80 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            has_modifier = eac_apm_focus_minerals_modifier
            habitability = { who = PREVPREV value >= 0.8 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_energy_hab_any = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            has_modifier = eac_apm_focus_energy_modifier
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_energy_hab_60 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            has_modifier = eac_apm_focus_energy_modifier
            habitability = { who = PREVPREV value >= 0.6 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_energy_hab_80 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            has_modifier = eac_apm_focus_energy_modifier
            habitability = { who = PREVPREV value >= 0.8 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_research_hab_any = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            has_modifier = eac_apm_focus_research_modifier
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_research_hab_60 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            habitability = { who = PREVPREV value >= 0.6 }
            has_modifier = eac_apm_focus_research_modifier
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_research_hab_80 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            habitability = { who = PREVPREV value >= 0.8 }
            has_modifier = eac_apm_focus_research_modifier
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_preferred_hab_80 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            has_modifier = eac_apm_planet_preference_modifier
            habitability = { who = PREVPREV value > 0.8 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet Preferred: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_preferred_hab_60 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            has_modifier = eac_apm_planet_preference_modifier
            habitability = { who = PREVPREV value > 0.6 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet Preferred: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_preferred_hab_any = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            has_modifier = eac_apm_planet_preference_modifier
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet Preferred: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_hab_100 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            habitability = { who = PREVPREV value >= 1.0 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_hab_90 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            habitability = { who = PREVPREV value >= 0.9 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_hab_80 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            habitability = { who = PREVPREV value >= 0.8 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet1 00: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_hab_70 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            habitability = { who = PREVPREV value >= 0.7 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_hab_60 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            habitability = { who = PREVPREV value >= 0.6 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_hab_50 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            habitability = { who = PREVPREV value >= 0.5 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_hab_40 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            habitability = { who = PREVPREV value >= 0.4 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_hab_30 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            habitability = { who = PREVPREV value >= 0.3 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_hab_20 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            habitability = { who = PREVPREV value >= 0.2 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_hab_10 = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
            habitability = { who = PREVPREV value >= 0.1 }
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}

eac_try_move_pop_planet_hab_any = {
    random_owned_planet = {
        limit = {
            eac_is_planet_resettlement_target = yes
        }
        resettle_pop = { pop = PREVPREV planet = this }
        #log = "---- New Planet 100: [This.GetName]"
        prev = { set_variable = { which = planet_found value = 1 } }
    }
}