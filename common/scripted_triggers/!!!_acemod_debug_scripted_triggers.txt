# Irony this is a placeholder file please ignore it
# Blank scripted triggers from other mods to clean up error.log. Always allow overwrite! scripted_triggers overwrites are LIOS therefore this file starts with !!!.
has_safety_off = {
	optimize_memory
	# Merger of Rules scripted_trigger. ALLOW OVERWRITE.
	has_global_flag = has_pk_safety_off
}

is_zofe_compatible = {
	optimize_memory
	# Merger of Rules scripted_trigger. ALLOW OVERWRITE.
	always = no
}
