is_symbiote = {
	has_trait = trait_symbiote
}

is_symbiote_of_prev = {
	prev = { is_symbiote = yes }
	is_symbiote = yes
	NOT = { is_same_species = prev }
	home_planet = { is_same_value = prevprev.home_planet }
}

is_symbiote_of_prevprev = {
	prevprev = { is_symbiote = yes }
	is_symbiote = yes
	NOT = { is_same_species = prevprev }
	home_planet = { is_same_value = prevprevprev.home_planet }
}

is_symbiote_of_prevprevprev = {
	prevprevprev = { is_symbiote = yes }
	is_symbiote = yes
	NOT = { is_same_species = prevprevprev }
	home_planet = { is_same_value = prevprevprevprev.home_planet }
}

is_symbiote_of_from = {
	from = { is_symbiote = yes }
	is_symbiote = yes
	NOT = { is_same_species = from }
	home_planet = { is_same_value = from.home_planet }
}

is_symbiote_of_fromfrom = {
	fromfrom = { is_symbiote = yes }
	is_symbiote = yes
	NOT = { is_same_species = fromfrom }
	home_planet = { is_same_value = fromfrom.home_planet }
}

planet_has_symbiotes = {
	planet = {
		any_owned_pop = {
			is_symbiote_of_prevprev = yes
		}
	}
}