# FROM = The targeted country
# THIS/ROOT = The acting country
wg_projcrim_seize_cure = {
	casus_belli = cb_projcrim_cure
	
	hide_if_no_cb = yes
	total_war = no
	
	allowed_peace_offers = {
		status_quo
		surrender
		demand_surrender
	}

	potential = {
		has_total_war_cb = no
		is_country_type = default
		has_event_chain = "projcrim_main_chain"
		NOT = {
			has_country_flag = projcrim_plague_cured
		}
		from = {
			is_country_type = default
			has_country_flag = projcrim_plague_cured
		}
	}
	possible = {
		always = yes
	}
	on_status_quo = {		
	}
	on_accept = {
		custom_tooltip = wg_projcrim_seize_cure_accept
		custom_tooltip = wg_humiliation_accept_projcrim
		hidden_effect = {		
			root = {	# Set cure distribution situation
				if = {
					limit = {
						NOT = {
							has_country_flag = projcrim_sit3_started
						}
					}
					start_situation = {
						type = projcrim_cure_distribution
						target = root
					}
					set_country_flag = projcrim_sit3_started
				}
			}
			hidden_effect = {
				from = {
					add_modifier = { modifier = humiliated days = 3600 }
				}
			}
		}
	}
	
	ai_weight = {
		weight = 5
	}
}