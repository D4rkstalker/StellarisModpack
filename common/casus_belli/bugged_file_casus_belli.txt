###HIVE###
cb_proliferation = {
	show_in_diplomacy = no
	show_notification = no
	potential = {
		has_valid_civic = civic_aggressive_proliferation
	}
	is_valid = {
		has_total_war_cb = no
		NOT = { is_overlord_to = FROM }
		OR = {
			is_subject = no
			overlord = { NOT = { is_same_value = from } }
		}
	}
}


###MACHINE###

cb_emancipation = {
	show_in_diplomacy = no
	show_notification = no
	potential = {
		has_valid_civic = civic_emancipation_proclamation
	}
	is_valid = {
		has_total_war_cb = no
		NOT = { is_overlord_to = FROM }
		OR = {
			is_subject = no
			overlord = { NOT = { is_same_value = from } }
		}
	}
}

###MEGACORP###

cb_appropriation = {
	show_in_diplomacy = no
	show_notification = no
	potential = {
		is_country_type = default
		has_valid_civic = civic_asset_appropriation
	}
	is_valid = {
		has_total_war_cb = no
		NOT = { is_overlord_to = FROM }
		OR = {
			is_subject = no
			overlord = { NOT = { is_same_value = from } }
		}
	}
}