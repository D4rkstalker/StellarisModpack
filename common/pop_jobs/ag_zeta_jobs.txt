
ag_zeta_crisis_purge = {
	icon = purge
	category = purge
	purge = purge_normal		# purge_ag_absorb		# Can't purge with purge_ag_absorb for unknown reason.
	is_capped_by_modifier = no
	ignores_sapience = yes
	can_set_priority = no
	clothes_texture_index = 2
	
	possible_pre_triggers = {
		has_owner = yes
		has_planet = yes
	}
	
	possible = {
		exists = planet
		planet = { exists = controller }
		planet.controller = { is_country_type = ag_zeta_crisis_country }
	}

	resources = { category = planet_jobs produces = { unity = 3 sr_zro = 1 } }
	
	planet_modifier = {
		pop_growth_speed = -10.0
		planet_immigration_pull_mult = -10.0
		planet_emigration_push_add = 1000
		planet_resettlement_unemployed_destination_mult = -10.0
	}

	weight = { weight = 114514 }
}
