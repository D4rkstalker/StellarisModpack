projcrim_plague_attitude = {
	potential = {
		has_event_chain = projcrim_main_chain
		NOT = { has_country_flag = projcrim_plague_cured }
	} 

	option = {
		name = "projcrim_dismissive"

		policy_flags = {
			projcrim_dissmissive_attitude
		}

		potential = {
			is_gestalt = no
		}

		modifier = {
			planet_jobs_produces_mult = 0.05
			pop_happiness = -0.10
		}

		AI_weight = {
			factor = 0
		}
	}

	option = {
		name = "projcrim_dismissive_gestalt"

		policy_flags = {
			projcrim_dissmissive_attitude
		}

		potential = {
			is_gestalt = yes
		}

		modifier = {
			planet_jobs_produces_mult = 0.05
			planet_amenities_no_happiness_mult = -0.15
		}

		AI_weight = {
			factor = 0
		}
	}

	option = {
		name = "projcrim_concerned"

		policy_flags = {
			projcrim_concerned_attitude
		}


		modifier = {
			planet_projcrim_conformity_mult = 0.05
			planet_jobs_produces_mult = -0.05
		}

		on_enabled = {
			hidden_effect = {
				every_owned_planet = {
					change_variable = {
						which = projcrim_world_conformity
						value = 5
					}
				}
			}
		}

		on_disabled = {
			hidden_effect = {
				every_owned_planet = {
					subtract_variable = {
						which = projcrim_world_conformity
						value = 5
					}
				}
			}
		}

		AI_weight = {
			factor = 10
		}
	}

	option = {
		name = "projcrim_compelled"

		policy_flags = {
			projcrim_compelled_attitude
		}

		potential = {
			is_gestalt = no
		}
		
		modifier = {
			planet_projcrim_conformity_mult = 0.10
			pop_happiness = 0.05
			planet_jobs_produces_mult = -0.15
		}

		on_enabled = {
			hidden_effect = {
				every_owned_planet = {
					change_variable = {
						which = projcrim_world_conformity
						value = 10
					}
				}
			}
		}

		on_disabled = {
			hidden_effect = {
				every_owned_planet = {
					subtract_variable = {
						which = projcrim_world_conformity
						value = 10
					}
				}
			}
		}

		AI_weight = {
			factor = 3
		}
	}

	option = {
		name = "projcrim_compelled_gestalt"

		policy_flags = {
			projcrim_compelled_attitude
		}

		potential = {
			is_gestalt = yes
		}

		modifier = {
			planet_projcrim_conformity_mult = 0.10
			planet_amenities_no_happiness_mult = 0.10
			planet_jobs_produces_mult = -0.15
		}

		on_enabled = {
			hidden_effect = {
				every_owned_planet = {
					change_variable = {
						which = projcrim_world_conformity
						value = 10
					}
				}
			}
		}

		on_disabled = {
			hidden_effect = {
				every_owned_planet = {
					subtract_variable = {
						which = projcrim_world_conformity
						value = 10
					}
				}
			}
		}

		AI_weight = {
			factor = 4
		}
	}
}