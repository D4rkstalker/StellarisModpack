automate_crime_management = {

	available = {
		planet_crime > 12
	}

	crisis = yes

	buildings = {
		# regular empire precints
		precinct = {
			building = building_precinct_house
			available = {
				NOR = {
					AND = {
						has_building = building_precinct_house
						owner = { has_technology = tech_colonial_centralization }
					}
					has_building = building_hall_judgment
					num_buildings = { type = building_precinct_house value >= 3 }
				}
			}
		}
		
		# gestalt
		sentinel_posts = {
			building = building_sentinel_posts
			available = {
				# should this be here? but crime is worse than a little deficit so probably not?
				# owner = {
				# 	sentinel_posts_upkeep_affordable = yes
				# }
				NOR = {
					AND = {
						has_building = building_sentinel_posts
						owner = { is_gestalt = yes }
					}
					num_buildings = { type = building_sentinel_posts value >= 3 }
				}
			}
		}

	}
}