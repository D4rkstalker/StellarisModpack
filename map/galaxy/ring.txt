# RING

core_radius_perc = 0				# Core radius is 30% of the galaxy radius

ring = {
	width 	= 0.2					# Percentage of radius to use as the width of the ring
	offset 	= 0.6					# Percentage of radius to use as the offset from center for the ring
}