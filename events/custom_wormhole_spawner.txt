namespace = custom_wormholes

event = {
	id = custom_wormholes.0
	hide_window = yes
	is_triggered_onlt = yes
	
	immediate = {
		every_system = {
			limit= { has_star_flag = custom_wormhole_1 }
			every_system = {
				limit= { has_star_flag = custom_wormhole_2 }
				prev = { # done this way to ensure we don't spawn "orphan" wormholes (wormholes that aren't linked to anything)
						spawn_natural_wormhole = {
						bypass_type = gateway_custom_wormhole
						random_pos = yes
						orbit_angle = 360
					}
				}
				spawn_natural_wormhole = {
					bypass_type = gateway_custom_wormhole
					random_pos = yes
					orbit_angle = 360
				}
			}
			
		}
	}
