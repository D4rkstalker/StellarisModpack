namespace = potv_patch

@potv_version = 0.11

#broadcast the latest version
event = {
	id = potv_patch.1
	hide_window = yes
	is_triggered_only = yes

	immediate = {
		event_target:complex_event_country = {set_variable = { which = latest_potv_version value = @potv_version }}
	}
}

#broadcast the latest version
event = {
	id = potv_patch.2
	hide_window = yes
	is_triggered_only = yes

	immediate = {
		event_target:complex_event_country = {set_variable = { which = latest_potv_version value = @potv_version }}
		event_target:complex_event_country = {set_complex_up_to_date = yes}
	}
}

