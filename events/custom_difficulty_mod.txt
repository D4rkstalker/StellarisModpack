namespace = custom_difficulty_mod

country_event = {
	id = custom_difficulty_mod.1
	hide_window = yes
	immediate = {
		add_modifier = { modifier = modifiers_ship_upgrade days = -1 }
	}
}
country_event = {
	id = custom_difficulty_mod.2
	hide_window = yes
	immediate = {
		remove_modifier = modifiers_ship_upgrade
	}
}