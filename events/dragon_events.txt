namespace = dragon_event

country_event = {
	id = dragon_event.1
	hide_window = yes
	is_triggered_only = yes

	trigger = {
		from = {
			species = {
				has_trait = trait_dragonbody
			}
		}
	}

	immediate = {
		from = {
			add_trait_no_notify = leader_trait_dragon
		}
	}	
}