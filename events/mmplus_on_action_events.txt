#####################################
# Mundane Machines Plus Events #
#####################################

namespace = mmplus

# Set global flag for compatibility, only triggers once
event = {
	id = mmplus.1
	hide_window = yes
	is_triggered_only = yes
	fire_only_once = yes
	
	immediate = {
		set_global_flag = mmplus_enabled_flag
	}
}
