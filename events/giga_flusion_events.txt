namespace = gigaflusion

country_event = {
	id = gigaflusion.1
	hide_window = yes
	is_triggered_only = yes

	immediate = {
		random_planet = {
			limit = {
				has_planet_flag = gigaflusion
			}
			set_planet_entity = {
				entity = "flusion_planet_entity"
			}
		}
	}
}