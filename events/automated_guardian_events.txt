namespace = automated_guardian_event
country_event = {
	id = automated_guardian_event.1
	hide_window = yes

	is_triggered_only = yes

	trigger = {
		has_origin = origin_automated_guardian
	}

	immediate = {
		random_owned_fleet = {
			limit = {
				is_ship_size = corvette
			}
			dismantle = yes
		}
		add_modifier = {
			modifier = automated_guardian_origin_debuff
			days = 5475
		}
		create_fleet = {
			name = "NAME_Dreadnought"
			effect = {
			set_owner = prev
				create_ship = {
					name = random
					design = "NAME_Type_46" # Jump Dreadnought
					graphical_culture = "npf_01"
					upgradable = no
					}
				set_location = prev.capital_star
			}
		}
	}
}