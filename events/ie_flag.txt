namespace = ie_flag

event = {
	id = ie_flag.1
	hide_window = yes
    is_triggered_only = yes
	
	trigger = { NOT = { has_global_flag = ie_flag } }
	
	immediate = {
		if = {
			limit = {
				NOT =  { has_global_flag = ie_flag }
			}
			set_global_flag = ie_flag
		}
	}
}
