namespace = ipa_settings_options_allow_growth_buildings

# CountryEvent: ipa_settings_options_allow_growth_buildings.1
country_event = {
	id = ipa_settings_options_allow_growth_buildings.1
	title = ipa_settings_options_allow_growth_buildings.1.title
	desc = ipa_settings_options_allow_growth_buildings.1.desc
	hide_window = no
	is_triggered_only = yes

	# EventOption: ipa_settings_options_allow_growth_buildings.1.allowBoth
	option = {
		name = ipa_settings_options_allow_growth_buildings.1.allowBoth

		hidden_effect = {
			set_variable = { which = ipa_options_allow_growth_buildings value = 0 }
			country_event = { id = ipa_settings_options.1 }
		}

	}

	# EventOption: ipa_settings_options_allow_growth_buildings.1.allowOrganic
	option = {
		name = ipa_settings_options_allow_growth_buildings.1.allowOrganic

		hidden_effect = {
			set_variable = { which = ipa_options_allow_growth_buildings value = 1 }
			country_event = { id = ipa_settings_options.1 }
		}

	}

	# EventOption: ipa_settings_options_allow_growth_buildings.1.allowRobotic
	option = {
		name = ipa_settings_options_allow_growth_buildings.1.allowRobotic

		hidden_effect = {
			set_variable = { which = ipa_options_allow_growth_buildings value = 2 }
			country_event = { id = ipa_settings_options.1 }
		}

	}

	# EventOption: ipa_settings_options_allow_growth_buildings.1.forbidden
	option = {
		name = ipa_settings_options_allow_growth_buildings.1.forbidden

		hidden_effect = {
			set_variable = { which = ipa_options_allow_growth_buildings value = 3 }
			country_event = { id = ipa_settings_options.1 }
		}

	}

	# EventOption: ipa_settings_options_allow_growth_buildings.1.back
	option = {
		name = ipa_settings_options_allow_growth_buildings.1.back

		hidden_effect = {
			country_event = { id = ipa_settings_options.1 }
		}

	}

}

