namespace = legw_shipevent

############# Mirror Planet Events ############# root = ship, fromfromfrom = planet, this = ship
# Complete - Legendary
ship_event = {
	id = legw_shipevent.8
	title = legw_shipevent.8.name
	desc = legw_shipevent.8.desc
	picture = GFX_evt_dim_portal
	show_sound = event_ship_bridge
	location = root
	
	is_triggered_only = yes
	
	immediate = {
		fromfromfrom = {
			remove_modifier = "add_gateway"
			add_modifier = { modifier = dimensional_planet days = -1 }
			set_planet_flag = legw_planet
			add_deposit = "legw_d_dimensional_complete"
			
			set_name = {
				key = "NAME_legw_color_planet"
				variable_string = "[This.GetName]"
			}
			set_planet_flag = "pf_colored"
		}
	}
	
	option = {
		name = "legw_shipevent.8.a"
	}
}
# Mineral Moon
ship_event = {
	id = legw_shipevent.9
	title = legw_shipevent.9.name
	desc = legw_shipevent.9.desc
	picture = GFX_evt_dim_portal
	show_sound = event_ship_bridge
	location = root
	is_triggered_only = yes
	
	immediate = {
	}

	option = {
		name = legw_shipevent.9.a
		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = minerals
					value = 15
					min = 1200
					max = 4200
				}
			}
		}
		hidden_effect = {		
			fromfromfrom = { 
				remove_modifier = "add_gateway"
				add_deposit = "legw_d_dimensional_minerals"
			}
		}
	}

}
# Star - Energy
ship_event = {
	id = legw_shipevent.10
	title = legw_shipevent.10.name
	desc = legw_shipevent.10.desc
	picture = GFX_evt_dim_portal
	show_sound = event_ship_bridge
	location = root
	is_triggered_only = yes
	
	immediate = {
	}
	
	option = {
		name = "legw_shipevent.10.a"
		
		hidden_effect = {
			fromfromfrom = { 
				remove_modifier = "add_gateway"
				add_deposit = "legw_d_dimensional_star"
			}
		}
	}
}
# Galaxy - Research
ship_event = {
	id = legw_shipevent.11
	title = legw_shipevent.11.name
	desc = legw_shipevent.11.desc
	picture = GFX_evt_dim_portal
	show_sound = event_ship_bridge
	location = root
	
	is_triggered_only = yes
	
	immediate = {
	}
	
	option = {
		name = "legw_shipevent.11.a"
		hidden_effect = {
			fromfromfrom = { 
				remove_modifier = "add_gateway"
				add_deposit = "legw_d_dimensional_galaxy"
			}
		}
	}
}
# Closed - Rare Ressources
ship_event = {
	 id = legw_shipevent.12
	 title = legw_shipevent.12.name
	 desc = legw_shipevent.12.desc
	 picture = GFX_evt_dim_portal
	 show_sound = event_ship_bridge
	 location = root
	
	 is_triggered_only = yes
	
	 immediate = {
	 }
	
	 option = {
		 name = "legw_shipevent.12.a"

		 root = {
			 owner = {
				 add_monthly_resource_mult = {
					 resource = alloys
					 value = 10
					 min = 80
					 max = 800
				 }
			 }
		 } #End-Root
		 hidden_effect = {
			fromfromfrom = { 
				remove_modifier = "add_gateway"
				add_deposit = "legw_d_dimensional_broken"
			}
		}

	 }

 }
# Closed - Nothing / some Research
ship_event = {
	id = legw_shipevent.13
	title = legw_shipevent.13.name
	desc = legw_shipevent.13.desc
	picture = GFX_evt_dim_portal
	show_sound = event_ship_bridge
	location = root
	is_triggered_only = yes
	
	immediate = {
	}
	
	option = {
		name = legw_shipevent.13.a
		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = physics_research
					value = 20
					min = 600
					max = 9000
				}
			}
		} #End-Root

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = engineering_research
					value = 200
					min = 400
					max = 6000
				}
			}
		} #End-Root

		hidden_effect = {		
			fromfromfrom = { 
				remove_modifier = "add_gateway"
			}
		}
	}
	
	option = {
		name = legw_shipevent.13.b
		
		allow = {
			owner = { resource_stockpile_compare = { resource = minerals value >= 12050 } }
		}
		owner = {
			add_resource = { minerals = -12050 }
		}
		
		fromfromfrom = { #picks up the planet here, does not work in location under special project
			enable_special_project = {
			name = "legw_gateway_2nd"
			location = this
			owner = root.owner
			}
		}
		
		# needs to be removed here, if time runs out
		hidden_effect = {		
			fromfromfrom = { 
				remove_modifier = "add_gateway"
			}
		}
	}
	
		option = {
		name = legw_shipevent.13.c
		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = physics_research
					value = 10
					min = 600
					max = 1800
				}
			}
		} #End-Root

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = engineering_research
					value = 10
					min = 400
					max = 2100
				}
			}
		} #End-Root
		
		trigger = {
			owner = { resource_stockpile_compare = { resource = minerals value < 12050 } }
		}
		
		# needs to be removed here, if time runs out
		hidden_effect = {		
			fromfromfrom = { 
				remove_modifier = "add_gateway"
			}
		}
	}
}

############# Stations #############
# Mining station works - choice of energy/minerals
ship_event = {
	id = legw_shipevent.4
	title = legw_shipevent.4.name
	desc = legw_shipevent.4.desc
	picture = GFX_evt_orb_mining
	show_sound = event_ship_bridge
	location = from
	
	is_triggered_only = yes
	
	immediate = {
		from = {
			clear_deposits = yes
			add_modifier = { days = -1 modifier = "auto_mining" }
		}
	}
	
	option = {
		name = "legw_shipevent.4.a"
		from = { 
			add_deposit = d_automining_1
		}
	}
	option = {
		name = "legw_shipevent.4.b"
		from = { 
			add_deposit = d_automining_2
		}
	}
	option = {
		name = "legw_shipevent.4.c"
		allow = {
			owner = { resource_stockpile_compare = { resource = alloys value >= 850 } }
		}
		owner = {
			add_resource = { alloys = -850 }
		}
		from = { 
			add_deposit = d_automining_4
		}
	}
}

# Mining station fails - lower amount
ship_event = {
	id = legw_shipevent.14
	title = legw_shipevent.14.name
	desc = legw_shipevent.14.desc
	picture = GFX_evt_orb_mining
	show_sound = event_ship_bridge
	location = from # was root #works with root or from
	
	is_triggered_only = yes
	
	immediate = {
		from = { 
			clear_deposits = yes
			add_modifier = { days = -1 modifier = "auto_mining" }
		}
	}
	
	option = {
		name = "legw_shipevent.14.a"
		from = { 
			add_deposit = d_automining_3
		}
	}
}

# Nexus gets higher - add research
ship_event = {
	id = legw_shipevent.5
	title = legw_shipevent.5.name
	desc = legw_shipevent.5.desc
	picture = GFX_evt_orb_nexus
	show_sound = event_ship_bridge
	location = from

	is_triggered_only = yes
	
	immediate = {
		from = { 
			clear_deposits = yes
			add_modifier = { days = -1 modifier = "small_nexus" }
		}
	}
	
	option = {
		name = "legw_shipevent.5.a"
		from = { 
			add_deposit = d_smallnexus
		}
	}
	option = {
		name = "legw_shipevent.5.b"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = physics_research
					value = 50
					min = 15000
					max = 75000
				}
			}
		} #End-Root

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = engineering_research
					value = 50
					min = 8000
					max = 58000
				}
			}
		} #End-Root

	} # End-Option.b
}

# Nexus has failed - lower deposit
ship_event = {
	id = legw_shipevent.15
	title = legw_shipevent.15.name
	desc = legw_shipevent.15.desc
	picture = GFX_evt_orb_nexus
	show_sound = event_ship_bridge
	location = from

	is_triggered_only = yes
	
	immediate = {
		from = { 
			clear_deposits = yes
			add_modifier = { days = -1 modifier = "small_nexus" }
		}
	}
	
	option = {
		name = "legw_shipevent.15.a"
		from = { 
			add_deposit = d_smallnexus_fail
		}
	}
}

############# Ancients #############
# Ancients Homeworld found + Effects for Shields and Removing Shields
ship_event = {
	id = legw_shipevent.2
	title = "legw_shipevent.2.name"
	desc = "legw_shipevent.2.desc"
	picture = GFX_evt_pre_home
	show_sound = event_scanner
	location = from

	is_triggered_only = yes
	
	trigger = {
		from = {
			has_modifier = "pre_homeworld"
		}
	}

	option = {
		name = "legw_shipevent.2.a"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = unity
					value = 15
					min = 500
					max = 30000
				}
			}
		}

		hidden_effect = {
			if = {
				limit = { NOT = { from = { has_planet_flag = "pf_shielded" } } }
				from = {
					add_modifier = { modifier = "pre_shields" days = -1 }
				}
				from = { set_planet_flag = "pf_shielded" }
			}
		}

		enable_special_project = {
			name = "legw_lower_shields"
			location = from
			owner = root.owner
		}
	}

	option = {
		name = "legw_shipevent.2.b"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = energy
					value = 100
					min = 1200
					max = 40000
				}
			}
		}

		hidden_effect = {
			if = {
				limit = { NOT = { from = { has_planet_flag = "pf_shielded" } } }
				from = {
					add_modifier = { modifier = "pre_shields" days = -1 }
				}
				from = { set_planet_flag = "pf_shielded" }
			}
		}

		enable_special_project = {
			name = "legw_lower_shields"
			location = from
			owner = root.owner
		}
	}
}
# Special Project complete
ship_event = {
	id = legw_shipevent.55
	title = "legw_shipevent.55.name"
	desc = "legw_shipevent.55.desc"
	picture = GFX_evt_pre_home
	show_sound = event_alien_nature
	location = fromfrom
	
	is_triggered_only = yes

	option = {
		name = "legw_shipevent.55.a"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = physics_research
					value = 50
					min = 1100
					max = 16000
				}
			}
		} #End-Root
	}
}

# Ancients World found
ship_event = {
	id = legw_shipevent.61
	title = "legw_shipevent.61.name"
	desc = "legw_shipevent.61.desc"
	picture = GFX_evt_pre_home
	show_sound = event_scanner
	location = from

	is_triggered_only = yes
	
	trigger = {
		from = {
			has_modifier = "pre_world"
		}
	}

	option = {
		name = "legw_shipevent.61.a"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = unity
					value = 15
					min = 800
					max = 9000
				}
			}
		}
		
		hidden_effect = {
			if = {
				limit = { NOT = { from = { has_planet_flag = "pf_shielded" } } }
				from = {
					add_modifier = { modifier = "pre_shields" days = -1 }
				}
				from = { set_planet_flag = "pf_shielded" }
			}
		}

		enable_special_project = {
			name = "legw_lower_shields_b"
			location = from
			owner = root.owner
		}
	}

	option = {
		name = "legw_shipevent.61.b"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = influence
					value = 5
					min = 25
					max = 100
				}
			}
		}
		
		hidden_effect = {
			if = {
				limit = { NOT = { from = { has_planet_flag = "pf_shielded" } } }
				from = {
					add_modifier = { modifier = "pre_shields" days = -1 }
				}
				from = { set_planet_flag = "pf_shielded" }
			}
		}

		enable_special_project = {
			name = "legw_lower_shields_b"
			location = from
			owner = root.owner
		}
	}
}

ship_event = {
	id = legw_shipevent.56
	title = "legw_shipevent.56.name"
	desc = "legw_shipevent.56.desc"
	picture = GFX_evt_pre_home
	show_sound = event_alien_nature
	location = fromfrom
	
	is_triggered_only = yes

	option = {
		name = "legw_shipevent.55.a"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = physics_research
					value = 50
					min = 700
					max = 12000
				}
			}
		} #End-Root
	}
}

# Legendary Utopia found
ship_event = {
	id = legw_shipevent.62
	title = "legw_shipevent.62.name"
	desc = "legw_shipevent.62.desc"
	picture = GFX_evt_legw_utopia
	show_sound = event_scanner
	location = from

	is_triggered_only = yes
	
	trigger = {
		from = {
			has_modifier = "legw_utopia"
		}
	}

	option = {
		name = "legw_shipevent.61.a"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = unity
					value = @tier3unityreward
					min = @tier2unitymin
					max = @tier2unitymax
				}
			}
		}
		
		enable_special_project = {
			name = "legw_utopia_found"
			location = from
			owner = root.owner
		}
	}

	option = {
		name = "legw_shipevent.61.b"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = influence
					value = 14
					min = 80
					max = 250
				}
			}
		}
		
		enable_special_project = {
			name = "legw_utopia_found"
			location = from
			owner = root.owner
		}
	}
}

# special project utopia finished
ship_event = {
	id = legw_shipevent.57
	title = "legw_shipevent.57.name"
	desc = "legw_shipevent.57.desc"
	picture = GFX_evt_legw_utopia
	show_sound = event_alien_nature
	location = root
	
	is_triggered_only = yes

	option = {
		name = "legw_shipevent.57.a"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = engineering_research
					value = 50
					min = 700
					max = 12000
				}
			}
		} #End-Root
		
		hidden_effect = {
			if = {
				limit = { NOT = { fromfromfrom = { has_planet_flag = "pf_legwmod_set" } } }
				fromfromfrom = {
					add_modifier = { modifier = "legw_utopia_buff" days = -1 }
				}
				fromfromfrom = { set_planet_flag = "pf_legwmod_set" }
			}
		}
	}
	
	option = {
		name = "legw_shipevent.57.b"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = alloys
					value = 10
					min = 1100
					max = 38000
				}
				add_monthly_resource_mult = {
					resource = engineering_research
					value = 10
					min = 600
					max = 22000
				}
			}
		} #End-Root
		
		# Only for text to show, no effects!
		prev = {
			add_modifier = {
				modifier = legw_emp_utopia #empire modifier
				days = -1
			}
		}
		hidden_effect = { owner = { country_event = { id = legw_shipevent.577 } } }
		hidden_effect = {
			if = {
				limit = { NOT = { fromfromfrom = { has_planet_flag = "pf_legwmod_set" } } }
				fromfromfrom = {
					add_modifier = { modifier = "legw_utopia_nerf" days = -1 }
					remove_modifier = legw_utopia
				}
				fromfromfrom = { set_planet_flag = "pf_legwmod_set" }
			}
		}
	}
	
	option = {
		name = "legw_shipevent.57.c"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = physics_research
					value = 10
					min = 2400
					max = 48000
				}
				add_monthly_resource_mult = {
					resource = society_research
					value = 10
					min = 2600
					max = 50000
				}
				add_monthly_resource_mult = {
					resource = engineering_research
					value = 10
					min = 2800
					max = 49000
				}
			}
		} #End-Root
	}
}

# Hidden - creates empire buff
country_event = {
	id = legw_shipevent.577
	title = legw_shipevent.577.name
	
	hide_window = yes

	# location = from

	is_triggered_only = yes

	immediate = {
		add_modifier = {
			modifier = legw_emp_utopia
			days = -1
		}
	}
	
}
############# Treasure Event #############
# Starts special project for treasure event
# inactive at the moment
ship_event = {
	id = legw_shipevent.98
	title = "legw_shipevent.98.name"
	desc = "legw_shipevent.98.desc"
	picture = GFX_evt_derelict_interior
	show_sound = event_scanner
	location = from

	is_triggered_only = yes

	trigger = {
		from = {
			has_modifier = "legw_treasure"
		}
	}

	option = {
		name = "legw_shipevent.98.a"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = unity
					value = 150
					min = 50
					max = 50
				}
			}
		}

		enable_special_project = {
			name = "legw_treasure_found"
			location = from
			owner = root.owner
		}

	}

}

# Special Project complete
ship_event = {
	id = legw_shipevent.97
	title = "legw_shipevent.97.name"
	desc = "legw_shipevent.97.desc"
	picture = GFX_evt_derelict_interior
	show_sound = event_scanner
	location = fromfrom
	
	is_triggered_only = yes

	option = {
		name = "legw_shipevent.97.a"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = physics_research
					value = 10
					min = 7000
					max = 80000
				}
			}
		} #End-Root
	}
	option = {
		name = "legw_shipevent.97.b"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = alloys
					value = 10
					min = 3500
					max = 7500
				}
			}
		} #End-Root
	}
	option = {
		name = "legw_shipevent.97.c"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = energy
					value = 100
					min = 12000
					max = 27000
				}
			}
		} #End-Root
	}
}

############# Gateway Start Event #############
# Gateway Starter: Scoutet starts special project
ship_event = {
	id = legw_shipevent.99
	title = "legw_shipevent.99.name"
	desc = "legw_shipevent.99.desc"
	picture = GFX_evt_dim_portal
	show_sound = event_scanner
	location = from

	is_triggered_only = yes

	trigger = {
		from = {
			has_modifier = "add_gateway"
		}
	}

	option = {
		name = "legw_shipevent.99.a"

		root = {
			owner = {
				add_monthly_resource_mult = {
					resource = unity
					value = 5
					min = 150
					max = 1500
				}
			}
		}

		enable_special_project = {
			name = "legw_gateway_found"
			location = from
			owner = root.owner
		}

	}

}