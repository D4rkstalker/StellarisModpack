################################
#
# Arcomod Events
#
###############################

namespace = arcomod

### Arcomod setup
event = {
	id = arcomod.1
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		# ArcoMod
		every_playable_country = {
			limit = { has_origin = origin_jph_arcology }
			country_event = { id = arcomod.10 }
		}
	}
}
# Set Up
country_event = {
	id = arcomod.10
	hide_window = yes

	is_triggered_only = yes
	
	immediate = {
		
		capital_scope = {
			
			set_planet_size = 22
			clear_deposits = yes
			remove_all_districts = yes
			
			change_pc = pc_city
			set_planet_entity = {
				entity = "city_planet_entity"
				graphical_culture = owner
			}
			
			add_deposit = d_hydroponic_arcology_arcomod
			add_deposit = d_overloaded_infrastructure_arcomod
			add_deposit = d_plentiful_stockpile_arcomod
			add_deposit = d_arcomod_1
			add_deposit = d_arcomod_2
			add_deposit = d_arcomod_3
			
			add_district = district_arcology_housing
			add_district = district_arcology_housing
			add_district = district_arcology_housing
			if = {
				limit = { owner = { is_megacorp = no } }
				#add_building = building_commercial_zone
			}
			
		}
	}
}