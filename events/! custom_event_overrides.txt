namespace = debug_fixes

country_event = {
	id = debug_fixes.1
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		every_owned_pop = {
			limit = { 
				NOT = {
					is_same_species = root
					is_robot_pop = yes
				}
			}
			kill_pop = yes 
			root = {
				create_pop = {
					species = root.owner
					ethos = root.owner
				}
			}
			
		}
	}
}