namespace = legw_game_start

# Normal Planets, habitable, no homeworlds, no fallen empire colonies, no primitives/presapients
event = {
	id = legw_game_start.0
	hide_window = yes
	fire_only_once = yes
	is_triggered_only = yes
	
	immediate = {
		every_galaxy_planet = {
			if = {
				limit = {
					legw_planet_habitable = yes
					NOR = {
						is_colony = yes
						is_capital = yes

						AND = {
							exists = owner
							legw_fallen_empire_planet = yes
							OR = {
								is_colony = yes
								is_capital = yes
							}
						}
					}
				}
			
				planet_event = { id = legw_spawn_event.0 days = 5 }	# Call for Modifier Event
				#planet_event = { id = lege_spawn_event.0 days = 6 }	

			} #ifend
				
				
		} #everyplanetend
	} #immediateend
}

# ONLY fallen empires
event = {
	id = legw_game_start.5
	hide_window = yes
	fire_only_once = yes
	is_triggered_only = yes
	
	immediate = {
		every_galaxy_planet = {
			if = {
				limit = {
					AND = {
						exists = owner
						legw_fallen_empire_planet = yes
						# has_planet_flag = "fallen_empire_world"
						OR = {
							is_colony = yes
							is_capital = yes
						}
					}
				}
				planet_event = { id = legw_spawn_event.5 days = 7 }	# Call for Modifier Event
			}
				
				
		} #everyplanetend
	} #immediateend
}

# ONLY primitives/presapients
event = {
	id = legw_game_start.1
	hide_window = yes
	fire_only_once = yes
	is_triggered_only = yes
	
	immediate = {
		every_galaxy_planet = {
			if = {
				limit = {
					exists = owner
					legw_primitive_planet = yes
				}
				planet_event = { id = legw_spawn_event.1 }	# Call for Modifier Event
			}
				
				
		} #everyplanetend
	} #immediateend
}

# ONLY non-habitable
event = {
	id = legw_game_start.2
	hide_window = yes
	fire_only_once = yes
	is_triggered_only = yes
	
	immediate = {
		every_galaxy_planet = {
			if = {
				limit = {
					legw_planet_uninhabitable = yes
				}
				planet_event = { id = legw_spawn_event.2 days = 5}	# Call for Modifier Event

			}
							
		} #everyplanetend
	} #immediateend
}

# ONLY megastructures, no homeworlds, no fallen empires
event = {
	id = legw_game_start.3
	hide_window = yes
	is_triggered_only = yes
	
	immediate = {
		every_galaxy_planet = {
			if = {
				limit = {
					#AND = {
						legw_megaplanet = yes
						NOR = {
							is_capital = yes
							AND = {
								exists = owner
								legw_fallen_empire_planet = yes
								OR = {
									is_colony = yes
									is_capital = yes
								}
							}
						}
					#}
				}
			
				planet_event = { id = legw_spawn_event.4 }	# Call for Modifier Event
			} #ifend
				
				
		} #everyplanetend
	} #immediateend
}

# ONLY Tombstart
event = {
	id = legw_game_start.7
	hide_window = yes
	fire_only_once = yes
	is_triggered_only = yes
	
	immediate = {
		every_galaxy_planet = {
			if = {
				limit = {
					has_modifier = "legw_tombstart_planet"
				}
				planet_event = { id = legw_spawn_event.7 days = 2}	# Call for Event (needs a delay because starting mods are changing pc_nuke classes away
			}
							
		} #everyplanetend
	} #immediateend
}

event = {
	id = legw_game_start.8
	hide_window = yes
	fire_only_once = yes
	is_triggered_only = yes
	
	immediate = {
		every_galaxy_planet = {
			if = {
				limit = {
					has_modifier = "legw_tombstart_planet_hw"
				}
				planet_event = { id = legw_spawn_event.8 }	# Call for Event
				planet_event = { id = legw_spawn_event.9 days = 1}	# Call for Event (needs a delay because starting mods are changing pc_nuke classes away)
			}
							
		} #everyplanetend
	} #immediateend
}