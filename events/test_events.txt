namespace = test
# country_event = {
	# id = test.100
	# title = "test country establishment"
	# desc = "establish communication with Hekoray"
	# picture = GFX_evt_psionics
	# show_sound = event_psionic
	
	# trigger = {
		# NOT = {has_country_flag = test_country_communication}
	# }

	# immediate = {
		# set_country_flag = test_country_communication
		# random_country = {
			# limit = { is_country_type = hekoray }
			# save_event_target_as = the_hekoray
		# }
	# }

	# option = {
		# name = "utopia.2656.a"
		# establish_communications_no_message = event_target:the_hekoray
		# hidden_effect = {
			# set_country_flag = hekoray_intro
			# country_event = { id = test.200 }
		# }
	# }
# }

country_event = {
	id = test.200
	title = test.200.name
	hide_window = yes
	trigger = {
		NOT = {has_country_flag = shroud_expanded}
	}
	immediate = {
		set_country_flag = shroud_expanded
	}
}

# country_event = {
	# id = test.10
	# title = test_of_kaolon
	# desc = test_of_kaolon
	
	# trigger = {NOT = {has_country_flag = yindiram_test}}

	# immediate = {
		# set_country_flag = yindiram_test
	# }

	# option = {
		# name = OK
		# create_fleet = {
			# name = "Kaolon"
			# settings = { 
				# can_upgrade = no 
				# can_change_composition = no 
				# can_change_leader = no 
				# spawn_debris = no
			# }			
			# effect = {
				# set_owner = root
				# create_ship = {
					# name = "Kaolon"
					# design = "KAOLON"
					# prefix = no
					# upgradable = no
				# }
				# set_location = root.capital_scope
			# }
		# }
		# default_hide_option = yes
	# }
# }



