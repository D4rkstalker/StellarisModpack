## Mod by Terak_Lapis
## Orig by Terak_Lapis

namespace = paperpop

## Paper Pope created incase NOT already done after Tech researched
country_event = {	
	id = paperpop.100
	is_triggered_only = yes 
	hide_window = yes 
	trigger = {	
		has_ascension_perk = ap_paper_popacy
		NOT = {
			has_global_flag = paper_pope_emerged
		}
	}
	immediate = {
		country_event = {
			id = paperpop.101
		}
	}
}

country_event = {	# Creating the Paper Pope
	id = paperpop.101
	title = "paperpop.101.title"
	desc = "paperpop.101.desc"
	show_sound = event_mystic_reveal
	picture = GFX_evt_binary_stars
	is_triggered_only = yes
	location = capital_scope
	hide_window = no
	trackable = yes

	trigger = {
		has_technology = tech_paper_pope
	}

	immediate = { #Creates the Paperpope as an Gov.
		create_leader = {	
			class = governor
			species = owner_main_species
			name = "NAME_PAPER_POPE"
			skill = 9
			traits = {	
				trait = leader_trait_governor_chosen
				trait = leader_trait_governor_pappop
			}  
		}
		last_created_leader = {
			save_event_target_as = paper_pope_person
		}
		hidden_effect = {
			set_country_flag = pp_rebellion_incoming
		}
	}

	option = {	
		name = EXCELLENT
		hidden_effect = { 
			set_global_flag = paper_pope_emerged 
			set_country_flag = paperpop_emerged
		}
	}
}



##Planned Event Chains
## Petition Wars


country_event = { #Grants the Digsite Paperpope Reward
	id = paperpop.800
	hide_window = yes
	is_triggered_only = yes

	immediate = {
		add_relic = r_ppsite_reward 
	}

	trigger = {
		always = no
		NOT = {
			has_relic = r_ppsite_reward
			
		}
	}
}

country_event = { # Granting the Relic of the Paper Pope
	id = paperpop.2
	hide_window = yes
	show_sound = event_mystic_reveal
	is_triggered_only = yes
	

	trigger = {
		has_ascension_perk = ap_paper_popacy
		always = yes
		NOT = {
			has_relic = r_paper_pope
		}
	
	}

	immediate = {
		large_artifact_reward = yes
		add_relic = r_paper_pope
		#pop_ethic_spiritualist_attraction_mult = 0.35
	}
}